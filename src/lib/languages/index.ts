import de from './de';
import en from './en';
import fr from './fr';
import it from './it';
import pt from './pt';

export type Language = 'de' | 'en' | 'fr' | 'it' | 'pt';

export const availableLanguages = ['de', 'en', 'fr', 'it', 'pt'];

export default {
  de,
  en,
  fr,
  it,
  pt,
};
